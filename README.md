# Nativa
docker compose up --build
docker builder prune -f
docker compose logs "microservicio"
.\mvnw.cmd clean package -DskipTests

    "correo": "vcortez6565@gmail.com",
    "password": "87876565Vc."
