spring:
  application:
    name: menu-service
  datasource:
    url: jdbc:mysql://localhost:3306/db_menu_service
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8081

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
